# =============================================================================
# Docker Environment Configuration
# =============================================================================
# Copy this file to .env and fill in the values before running docker-compose
#
# Usage:
#   cp .env.docker.example .env
#   # Edit .env with your values
#   docker-compose up -d
# =============================================================================

# =============================================================================
# Database Configuration (Required)
# =============================================================================
# PostgreSQL password - REQUIRED, no default
POSTGRES_PASSWORD=your_secure_password_here

# PostgreSQL user (default: trading)
POSTGRES_USER=trading

# PostgreSQL database name (default: trading_core)
POSTGRES_DB=trading_core

# =============================================================================
# Trading Configuration
# =============================================================================
# Symbols to subscribe to (comma-separated)
TRADING_SYMBOLS=BTCUSDT,ETHUSDT

# =============================================================================
# Exchange API Keys (Optional - for authenticated endpoints)
# =============================================================================
# Binance API credentials (required for authenticated endpoints)
BINANCE_API_KEY=
BINANCE_API_SECRET=

# Databento API key (required for historical data fetching)
DATABENTO_API_KEY=

# =============================================================================
# Logging Configuration
# =============================================================================
# Log level configuration
# Format: crate=level,crate2=level2
# Levels: trace, debug, info, warn, error
RUST_LOG=data_manager=info,sqlx=warn

# Log output format: pretty, compact, or json
# Use 'json' for production/HTML client consumption
LOG_FORMAT=json

# =============================================================================
# Host Trading-Core Configuration
# =============================================================================
# These are used when running trading-core on the host machine
# The DATABASE_URL should point to the Docker container's exposed port

# Database URL for host applications (trading-core)
# Note: Use 'localhost' since TimescaleDB exposes port 5432 to host
DATABASE_URL=postgresql://trading:your_secure_password_here@localhost:5432/trading_core

# Redis URL for host applications
REDIS_URL=redis://127.0.0.1:6379

# Run mode: development, production, test
RUN_MODE=development
